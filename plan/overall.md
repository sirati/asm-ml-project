
DO NOT EDIT THIS FILE, human only

The project is split into different modules to allow me to cleanly separate concerns and allow easier configuration and hyperparameter search.

1. Data Loading and tensorization logic
2. optional ML preprocessing layer
3. Overall Model architecture (transformer vs perceiver-io like)
   1. Encoder part
   2. Decoder part
4. Active training objectives
   - they each use a separate instance of the Decoder part
5.


importantly we want to be able to configure what algorithm is used at layers, so e.g. the perceiver-io thinking part should have an argument what layer module we use e.g. attn, flash-attm, mamba etc, same goes for any other part.


the transformer part should allow different number of layers between encoder and decoder. in general is has been shown that the decoder part can have much much less layers, and just get as input the top N layers of the encoder.e.g. encoder is 10 layers, decoder is 4 layers, top two layers of encoder are cross attended by decoders bottom two layers.

please create a abc for self attention layer and cross attention layer builder. the configuration then provides these builders.
